#-------imports------#
from tkinter import *
import random
#--------------------#
#----------Window tings-------------#
window = Tk()
window.title("DICE ROLLER")
window.configure(background="black")
#----------------------------------#

#-------------------------------Def Functions-------------------------------------#
def roll ():
    a = Random_Number =random.randint(1,6)
    Label(text="You Rolled a: ",bg="black",fg="red").grid(row=3,column=1)
    Label(text= Random_Number,bg="black",fg="red").grid(row=3,column=2)
#---------------------------------------------------------------------------------#


#-----------------------------------Labels----------------------------------------#
Label(text="DICE ROLLER", bg="black", fg="red").grid(row=1,column=1)

#---------------------------------------------------------------------------------#

#-------------------------Buttons------------------------#
Button(text="Roll",command= roll).grid(row=2,column=1)
#--------------------------------------------------------#
window.mainloop()
